// Project should hold generic modules, if possible.
//
// so that e.g. llvm ir can call assembly code. and the vm can figure otu which executor to spawn
//
// however, since the states are different that might be hard...
